{
  "name": "begin-app",
  "version": "0.0.0",
  "description": "A Begin app with the minimum necessary file structure",
  "scripts": {
    "start": "run-s sandbox",
    "sandbox": "npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "^1.5.8"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
